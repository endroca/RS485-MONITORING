const express = require('express');
const router = express.Router();

router.get('/slavesOnline', (req, res) => {
    //es.json(salvesOnline);
});

router.post('/updateSetting', (req, res) => {

});

module.exports = router;
